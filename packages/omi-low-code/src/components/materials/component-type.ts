export enum ComponentType {
    Button = 'button',
    Layout = 'layout',
    // 可以在这里扩展其他组件类型
  }
  
// 可以拓展一个EventType 枚举，用于表示组件的事件类型
export enum EventType {
  Click = 'click',
  Change = 'change',
  // 可以在这里扩展其他事件类型
}
